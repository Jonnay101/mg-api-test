import SFunc from '../functions/stateFunctions';

describe('function that gets presets from the db', () => {


    // it('should return an object', (done) => {
    //     const data = SFunc.getPresetList('/api/user1234/eq').then(data => expect(data).toEqual({}))
    //     done()  
    // });

    // it('should throw an error and return it in an object when the incorrect URI is used', (done) => {
    //     const data = SFunc.getPresetList('/incorrect/uri').then(data => {
    //         expect(data).toEqual({"error": [TypeError: 'Network request failed']}        );
    //         expect(data).toThrowError()
    //     });
    //     done()
    // });

    // it('should not return object when the incorrect URI is used and the optional object is added', (done) => {
    //     const data = SFunc.getPresetList('/incorrect/uri', {dave: 'womble'}).then(data => {
    //         expect(data).not.toEqual({})
    //     });
    //     done()  
    // });
})